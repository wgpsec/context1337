apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cronjob-watcher.yaml
  # Note: do NOT include secret.example.yaml here.
  # Create the real Secret manually before applying:
  #   kubectl create secret generic ghcr-readonly-token \
  #     --from-literal=token=<GITHUB_PAT> -n <your-namespace>
